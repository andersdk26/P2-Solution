/* Should return a similarityscore for a group
used for weights contentbased % + collaborative % */


CalculateGroupDiversity(contentBasedFiltering) {
    const DiversityMovies: movie[] = [];
   for (UserID in Group) {
       contentBasedFiltering(UserID);
           DiversityMovies.push(m);
       }
       for (movieID in DiversityMovies ) {
           SELECT genre FROM tabel;
               DiversityMovies.push(m);
       }
       for (genre in DiversityMovies) {
           DiversityMovies.count();
           DiversityMovies.Sort();
       }
       Similarity() => {}
   }
   